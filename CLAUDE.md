# Cardano note — 公開動画の読み解きアーカイブ

Cardano まわりの**公開されている動画**（Midnight・IOG・Charles Hoskinson のチャンネルなど、1 本 1 時間前後のもの）の文字起こしから、1 本につき 3 ページの HTML を生成するリポジトリ。

**姉妹リポジトリ** — RealFi のコミュニティコールは `shiodome47/2026RealFi`（<https://shiodome47.github.io/2026RealFi/>）にある。成果物の形式は同じ。**あちらは匿名化の規律があるクローズドなコールの記録、こちらは公開動画だけ。**RealFi の話題がこちらの動画で出たら、あちらの該当する回の節へリンクする（URL は変えない約束なので、アンカー付きで貼ってよい）。

## 最優先のルール：出典が公開されているものだけを扱う

このリポジトリは **Public**。**扱うのは、誰でも見られる場所に公開されている動画だけ。**

- **`meta.yml` の `chatham_house_rule` は常に `false`。**発言者は実名のまま扱い、固有名詞の誤変換は直す
- **クローズドなコール・登録制のウェビナー・ライブ配信で公開アーカイブが残っていないものは、ここには置かない。**そういうものが来たら、`2026RealFi` 側の匿名化の手順で扱うか、断る
- **動画の中に「Confidential」と明示された資料が映っていたら、その部分は再現しない。**公開動画に映っていても、資料そのものが配布物とは限らない。末尾の `.note` に、映っていたことと再現していないことを書く
- **迷ったら載せない。**Public なリポジトリに一度出したものは取り消せない

## 新しい動画を追加するときの手順

「これは Midnight の YouTube の文字起こし、よろしく」と URL と字幕ファイルを渡されたら、**この手順で最後まで作る。**途中で判断がつかないことがあれば、そこだけ聞く。

### 1. 原本を保存する

```
episodes/YYYY-MM-DD-<slug>/
├── meta.yml
└── transcript.srt   ← 渡されたまま。無加工（.txt でもよい）
```

**渡されたテキストはそのまま置く。**誤字も改行もそのまま。ここが再生成のもとになる。固有名詞の修正は `docs/` 側でだけ行う。

`meta.yml` に書くもの。

```yaml
slug: 2026-09-05-midnight-dev-update
title: Midnight 開発アップデート — …（日本語の題）
title_en: Midnight developer update — …
date: 2026-09-05            # 動画の公開日。収録日ではない。分からなければ「要確認」とコメント
series: midnight            # shared/series.yml のキー（チャンネル）
chatham_house_rule: false   # 公開動画。常に false

platform: YouTube
channel: Midnight Network   # チャンネル名（表示名のまま）
official_channel: true      # 公式かどうかを**確認したか**。していないなら unknown と書く
duration: "58:12"
video: https://www.youtube.com/watch?v=...

speakers:
  - 名前                     # 動画の説明欄や画面の表示から。肩書は分かる範囲で
speaker_labels: inferred    # 字幕に話者ラベルがなければ inferred
timestamps: srt             # .srt なら時刻がある。全文ページの .turn-time に使う
proper_nouns_corrected_in_docs: true
```

**日付は公開日。**動画の中で「先週」「来月」と言われても、それは公開日を基準に読む。収録日が分かって公開日と大きく違うなら、両方書く。

### 2. `shared/` を読む

生成の前に必ず 3 つとも読む。

- `shared/glossary.md` — 用語の統一表記、音声認識の誤変換の修正表、翻訳方針
- `shared/speakers.md` — 話者の特定のしかた、過去の回で確度が低かった箇所
- `shared/open-questions.md` — 過去の回で保留になったこと。**今回の動画で答えが出ていないか確認する**

**RealFi に関わる話題なら、`2026RealFi` の `shared/open-questions.md` も見る。**あちらで「未回答」のものに、こちらの動画が答えていることがある。その場合はこちらの回の `補足` でリンクし、あちらのファイルは触らない（別リポジトリなので、依頼者に伝える）。

### 3. 3 つの HTML を生成する

```
docs/YYYY-MM-DD-<slug>/
├── index.html          ← メイン。日本語のわかりやすいまとめ（英語版も同じファイルに）
├── transcript-ja.html  ← 日本語全文
└── transcript-en.html  ← 英語全文（固有名詞の誤変換の修正以外は原文のまま）
```

3 ページとも `../assets/style.css` を参照し、末尾に `.docnav` を置いて互いにリンクする。

**index.html（まとめ）の構成**

1. `.thesis` — **「一言でいうと」**。この動画が結局何の話だったのかを 1〜2 文で。**ここが一番大事**
2. `.tldr` — 要点 5 つ前後
3. `.toc` — 目次（節が 6 つ以上なら必須）
4. 本編を話題ごとに再構成した見出し（時系列に縛られない。同じ話題が離れて出てくる場合はまとめる）
5. **全体像** — 話がどう組み合わさるのかを示す図で締める
6. Q&A — 動画に質疑があれば `.qa` カードにする。質問者は画面や字幕に出ている表示名で
7. 持ち帰りになった質問・答えなかった問い — `.qa.pending`
8. 末尾に `.note` — **動画へのリンク**、チャンネル名、公開日、出典の 3 区分（下の 4-b）の説明

**transcript-en.html（英語全文）**

- 話者を推定して `.turn` に分割する。**固有名詞の誤変換の修正以外は文言を変えない**。それ以外の誤変換はそのまま残す
- **1 人語りの動画は、話題の切れ目で `.turn` を分ける。**日本語全文と同じ分割にすること
- 推定が不確かな話者には `<em>(inferred)</em>` を付ける
- 話題の切れ目に `.sec` を挿入する。**`.turn-time` は動画の時刻**にする（`.srt` の時刻をそのまま使う）。まとめから該当箇所へ飛べるように、`.sec` に `id` を付ける

**transcript-ja.html（日本語全文）**

- 英語全文と**同じ `.turn` 分割・同じ `.sec` 区切り**にする。`tools/check.py` が数を突き合わせる
- 誤変換は `glossary.md` に従って修正する
- 不確かな話者には `<em>(推定)</em>` を付ける

### 4. 図解を入れる

**まとめページには図を入れる。**構造・順序・時間変化があるものは図にしたほうが速く伝わる。

インライン SVG で書き、`.fig` でラップして `.figcap` を付ける。外部ファイルや画像は使わない。

```html
<div class="fig">
<svg viewBox="0 0 680 250" role="img" aria-label="図の内容の説明">
  ...
</svg>
<p class="figcap">図の補足</p>
</div>
```

- 色は直接指定せず、用意済みのクラスを使う（`d-box` `d-box-accent` `d-box-warn` `d-box-plain` / `d-t` `d-s` `d-a` `d-w` / `d-line` `d-line-accent` / `d-head` `d-head-accent`）。ライト・ダーク両テーマに自動で追従する
- `viewBox` の幅は **680** に揃える。高さは内容次第
- 矢印マーカーの `id` は図ごとに一意にする（日本語 `m1` `m2` …／英語 `e1` `e2` …）
- `role="img"` と `aria-label` を必ず付ける

**文字が箱や viewBox からはみ出していないか、必ず確かめる。**SVG は文字が折り返さず、**後ろの箱の下に隠れる**。書いている側からは見えない。`tools/check.py` が字幅を概算して判定するので、**図を書いたら流す**。

はみ出したときは、**文字を削るより先に箱の側を動かす**。横に並べた箱の間にラベルを入れるなら箱の幅を詰めて隙間を広げる。長い引用は改行して 2 行にし、箱と `viewBox` の高さを一緒に増やす。**英語版は日本語版より横に長くなる**ので、日英で別のレイアウトにしてよい。

**動画の画面に映った図を写すときは、写したものだと分かるようにする。**`.figcap` に「動画 12:34 の画面をもとに描き直したもの」と書く。**動画の図をこちらで再構成したものと、こちらが考えた図は違う。**

### 4-b. 出典を 3 つに分ける

公開動画なので、**画面に映った資料は載せてよい**（Confidential の表示があるものを除く）。ただし**音声で語られたことと、画面にしかないことは区別する。**スライドのほうが音声より詳しいことが多く、混ぜると「話者がそう言った」ように読めてしまう。

| マーク | 意味 |
| --- | --- |
| （なし） | **動画の中で音声で語られた** |
| `画面` / `on screen` | **画面に映っていたが、口には出されていない**（スライド、デモ画面、図表） |
| `補足` / `added` | **こちらで足した。動画にはない** |

`<span class="badge">画面</span>` と書く。**末尾の `.note` で 3 つの違いを説明する。**画面の数字を読み取って書くときは、読み取りであることを添える。

**食い違ったら、両方書いて食い違いを事実として示す。**どちらかに寄せない。

### 4-c. 予測・時期・数字は、強く書かない

公開動画には、**価格・時期・採用数の予測**がよく出る。**このアーカイブは動画で語られたことの記録**であって、予測の転載でも勧誘でもない。

- **必ず出所を付ける。**「〜と述べられました」「本人の見方として〜」。**地の文で断定しない**
- **時期は、言われたとおりの確度で書く。**"hopefully Q4" を「Q4 に来る」にしない
- **数字は「何を数えたものか」と「いつ時点か」まで書く**
- **話者が留保していたら、その留保を落とさない**
- **利回りや価格の話は、仮の数字だと分かる言葉を数字のすぐ横に置く。**離れた場所の断り書きは読まれない

### 4-d. 本人のチャンネルの発言は「個人の見解」として書く

**Charles Hoskinson のチャンネルで語られた方針・時期・評価は、IOG や Cardano Foundation の公式見解と同じには扱わない。**「本人の見解として〜と述べています」の形にする。**組織の決定として書けるのは、公式チャンネルか公式文書が出典のときだけ。**

同じ話題を公式チャンネルと本人チャンネルの両方で扱っていたら、**両方の回をリンクして、言っていることの差分を `補足` で書く。**

### 4-e. 過去の回は書き換えない

**各回は、その時点で何が語られたかの記録。**あとで名前が変わっても、予定が変わっても、**過去の回の本文は置き換えない**。

- `episodes/` と全文ページは触らない
- まとめページも、その回の時点の内容のまま
- **動画のあとに判明したこと**（リリースされた、延期された、名前が変わった）は、**`補足` で「動画公開後の結果」と明示して**末尾か該当節に足す。マークなしで本文に混ぜると、動画で語られたことに見える
- 対応表や経緯は `shared/glossary.md` に 1 か所だけ置く

### 5-a. 専門用語が密な節は折りたたみで解説する

本文を長くすると読み通せなくなるが、用語が分からないと節ごと飛ばされる。**本文は短いまま、`<details class="explain">` に「かみくだいた説明」を入れる**。

```html
<details class="explain">
<summary>専門用語が多い節です — かみくだいた説明を開く</summary>
<div class="explain-body">
  <p><span class="badge">補足</span>以下はこちらで足した解説です。動画の発言ではありません。</p>
  <h4>そもそも「◯◯」とは</h4>
  ...
</div>
</details>
```

書き方 — ①用語そのものの意味 → ②なぜそれが要るのか → ③**身近なものへのたとえ** → ④一言でまとめ。たとえ話は 1 つに絞る。**中身は必ず `補足` 扱いにする。**

**「この動画では分からないこと」も書く。**説明が途中で止まっている箇所は、止まっていること自体が読者にとって重要な情報になる。`shared/open-questions.md` にも記録する。

### 5-b. 引っかかる主張には「素朴な疑問」の折りたたみを置く

**読んで素直に「それ本当？」と思う箇所は、思わせたまま通り過ぎさせない。**とくに、話者が**因果を断定している**箇所は、読者のほうが正しいことがある。

`<details class="explain">` の `summary` を **「素朴な疑問 — ◯◯なのでは？」** の形にして、①まず疑問が正しいことを認める → ②なぜ話者の言い方も成り立つのかを要素に分解する → ③外から見分ける方法を 1 つ挙げる → ④その数字が動画で示されたかどうかを書く。

**話者の主張は「〜と述べています」と帰属させる。**地の文で断定すると、こちらが裏を取ったように読まれる。

**用語カード（`.term`）で説明する語は、その前の本文に必ず出しておく。**動画の中の言葉なら本文で「〜と呼んでいます」と示し、こちらが足した言葉なら `補足` を付ける。

### 5. 動画の発言でない補足には印を付ける

理解を助けるために説明を足すのはよいが、**動画で言われたことと混ぜない**。足した内容には `<span class="badge">補足</span>` を付ける。例：リスクの指摘、仕組みを説明するための数値例、一般的な背景知識、他の回とのつながり。

### 6. `shared/` を更新する

- 新しい用語・誤変換パターンが出たら `glossary.md` に追記
- 今回の話者の特定と、確度が低かった箇所を `speakers.md` に追記
- 今回保留になった問いを `open-questions.md` に追記。過去の問いに答えが出ていたらステータスを更新

### 7. `docs/index.html` に 1 枚カードを追加する

新しい回が上に来るように挿入する。カードの説明文には「一言でいうと」の内容を入れる。

カードは**チャンネル（シリーズ）ごとにまとめる**。そのチャンネルの最初の回なら、`<h2>` の見出しと `series.yml` の `blurb` を使った 1 行の説明を、カードの前に置く。

```html
<h2><span class="l-ja">Midnight</span><span class="l-en">Midnight</span></h2>
<p class="meta"><span class="l-ja">（series.yml の blurb_ja）</span><span class="l-en">（blurb_en）</span></p>

<a class="card" href="./YYYY-MM-DD-slug/">
  <h2><span class="l-ja">題 — 一言でいうと</span><span class="l-en">Title — in one line</span></h2>
  <p class="meta">YYYY-MM-DD · YouTube · <span class="l-ja">チャンネル名 · 話者</span><span class="l-en">channel · speakers</span></p>
  <p><span class="l-ja">…</span><span class="l-en">…</span></p>
</a>
```

`.eyebrow` は**チャンネル名**（通し番号は振らない）。まとめ・日本語全文・英語全文の 3 ページとも同じにする。

## シリーズ（チャンネル）

`episodes/<slug>/meta.yml` の `series:` は、必ず `shared/series.yml` のキーにする。**先に series.yml へ書く。**`check.py` が、定義にないキーを落とす。

- **シリーズはチャンネルで切る。**番組名や話題（Leios など）でシリーズを作らない。話題は複数のチャンネルにまたがるので、`glossary.md` と `open-questions.md` で束ねる
- 新しいチャンネルが来たら、`series.yml` に `title_ja` / `title_en` / `blurb_ja` / `blurb_en` / `chatham_default: false` / `numbered: false` を書く
- 通し番号は振らない（`numbered: false`）。動画は 1 本ごとに独立した収録なので

## 話者の書き方

- **実名で書く。**動画の説明欄や画面の表示名を使う。肩書は分かる範囲で（「IOG の〜」「Midnight Foundation の〜」）
- **1 人語りの動画**では、まとめは「〜と述べています」の受動でよい。**複数人の動画**では、誰の発言かを書く（立場が違うと発言の重さが違う）
- **字幕の綴りが本人の綴りと違うことが多い。**特定できたら直し、`glossary.md` の誤変換表に載せる。**特定できなかった名前は聞こえたまま残し、まとめで「名前だけ挙がって説明されなかった」と事実として書く**
- **第三者への言及**（他プロジェクト、他の人物）も実名のまま。ただし**評価や批判が含まれるときは、話者の発言として帰属させる**。地の文にしない

## 完成条件（毎回チェックする）

**言われなくても毎回満たすこと。**ひとつでも欠けていたら未完成。

### 読みやすさ

長い文章は、それだけで読む気を削ぐ。**どこに目をやればいいかが一目で分かる状態**にする。

- [ ] `.thesis`（一言でいうと）がページ先頭にある
- [ ] `.tldr`（要点 5 つ前後）がある
- [ ] `.toc`（目次）がある。節が 6 つ以上なら必須
- [ ] **すべての `<h2>` に `id` と `.deck`（1 行の要約）が付いている**
- [ ] 節と節の間に十分な余白がある

**太字は「目印」であって「強調」ではない。**段落の半分が太字になった時点で目印としては機能しなくなり、**濃い塊＝文字の壁**として、読む前に離脱される原因になる。

- **1 段落・1 項目につき `<strong>` はふたつまで。**3 つ目を足したくなったら、段落を分ける合図
- **`.tldr` の各項目は「太字のリード＋普通の文」にする。**先頭に短い名札（`<strong>2 層のネットワーク。</strong>` のような体言止め）を置き、残りは太字なしで書く
- **長い一文をまるごと太字にしない。**太字にしてよいのは、視線が最初に落ちる数語まで
- **数字と否定は太字にしなくても目に入る**

`tools/check.py` が、太字が半分を超える段落を**気になるところ**として報告する（落としはしない）。必ず流して数を見る。

**`.thesis` と `.tldr` に、業界用語をそのまま置かない。**要点は、本文を読むかどうかを決める場所。ここで知らない言葉に当たると、そこで止まる。プロダクト名（Midnight、NIGHT、DUST、Leios など）は訳さない決まりなので出てよいが、**業界の中でしか通じない普通名詞**（sharding、finality、tranche など）は要点では平易な言葉に置き換え、**原語は本文の初出で渡す**（「動画ではこれを『〜』と呼んでいるので、以下もその言葉で書きます」）。置き換えた語は `glossary.md` の「訳し方」の欄に、要点でどう書くかまで書く。

### 言語

サイトは日本語と英語の両方で読める。**まとめは両言語で書く。**

- [ ] まとめページに `<div class="l-ja">` と `<div class="l-en" lang="en">` の両方がある。英語ブロックの終わりに `<!--/content-->` を置く
- [ ] `.lang-toggle` ボタンがある。初回訪問時は `navigator.language` で自動判定し、選択は `localStorage` に保存する
- [ ] **図の中の文字も英語版を用意する**
- [ ] 英語版の `<h2>` の `id` は `en-` を前置し、日本語版と重複させない。SVG の `marker` の `id` も分ける
- [ ] ヘッダー・ナビ・フッターの文言も `<span class="l-ja">` / `<span class="l-en">` で両方用意する
- [ ] `<title>` は両言語で通じる表記にする
- [ ] 全文ページは単一言語なので、そのページ自身の言語でナビを固定する。言語ボタンは対になるページへのリンクにする

英語版は日本語からの逐語訳にしない。**元の動画は英語なので、原文の言い回しをそのまま引用したほうが自然になる。**

### 配色

- [ ] 色は必ず CSS 変数経由で指定し、直値を書かない
- [ ] `prefers-color-scheme` と `:root[data-theme]` の両方に対応している
- [ ] `.theme-toggle` ボタンがあり、選択は `localStorage` に保存される

### 図解

- [ ] まとめページに図がある
- [ ] 図の色も CSS 変数経由で、両テーマに追従する

### その他

- [ ] ナビ（`.docnav`）は**ページ末尾**
- [ ] 動画の発言でない補足に `<span class="badge">補足</span>` が付いている
- [ ] 末尾の `.note` に動画へのリンクがある
- [ ] モバイル幅（560px 以下）でレイアウトが崩れない
- [ ] `aria-label` / `role` / `:focus-visible` が付いている
- [ ] `standalone.html` を再生成した

```
python3 tools/build_standalone.py            # 全回
python3 tools/build_standalone.py <slug>    # 1 回だけ
```

**まとめや全文を直したら必ず流す。**全文の 2 つは `<details>` で畳まれる（処理は `build_standalone.py` に入っている）。

### 機械的な検証

コミット前に必ず流す。

```
python3 tools/check.py
```

見ているもの — リンク切れ、`id` と SVG `marker` の重複、アンカーの飛び先、タグの閉じ忘れ、`deck` と目次の不足、図の `role`/`aria-label`、**図の文字のはみ出し・箱への重なり**、日英まとめの `.fig`/`.qa`/`.term`/`.deck` の数の一致、日英ブロックの取り違え、英日全文の `.turn`/`.sec` の一致、テーマトークン外の色の直書き、トグルの有無、シリーズの定義、太字の密度。

## 書き方のルール

- **断定していない発言は日本語でも断定しない**。"I think"、"probably"、"hopefully" は、ロードマップや時期の話で特に重要
- **プロダクト用語は訳さない**（Cardano, Midnight, NIGHT, DUST, Leios, Peras, Hydra, Mithril, EVM, DEX, SDK, testnet, mainnet など）
- 口語のフィラー（you know, I mean, like）は日本語訳では落とす
- **原文にない情報を足さない**。足すなら `補足` バッジを付けて区別する
- **初出の概念は説明を付ける。**業界用語は、読者が初めて見る前提で 1 行の説明を `補足` で足す。**言葉の指す範囲が誤解されやすいものは、何を含み何を含まないかまで書く**
- **「文字起こしからは分からない」と書かない。**制作過程の限界ではなく、**動画で何が語られ何が語られなかったかを事実として書く**（×「特定できません」→ ○「この一言のみで、続けて別の話題に移っています」）。`補足` バッジは出典の区別のためのもので、不確かさの印ではない
- **名前だけ挙がった外部の提案・プロジェクトは、背景を調べて補う。**読者が調べ直さずに済む状態にする。**数値と可決状況は必ず裏取りし、時点を明記する**
- **触れられただけで掘り下げられなかった話題も拾う。**掘り下げがなかったこと自体を明記する（推測で埋めない）
- **「はい／いいえ」の返事は、質問のどこを指しているか確かめる。**「A なのか、それとも B だけなのか」への "No" は B を否定していることが多い。返事だけを引かず、どちらを打ち消したのかまで書く
- **話者のたとえを図にするときは、たとえだと分かるようにする。**`.figcap` に「動画で語られた比喩をそのまま描いたもの」と書き、実際の構造と食い違う点は折りたたみで補足する
- **技術的な仕組みを説明するときは、成り立つ条件まで確認する。**「A なら必ず B」と書く前に反例を探す
- **「決まった」と「決まらなかった」の間を潰さない。**投票や承認の話では、正式に成立したのか、成立していないが判断材料として使われたのかを区別する
- **組織の顔ぶれは変わる。**「◯◯の 5 社」のように書くときは、必ず時点を添える。**公開日の時点でどうだったか**と、**今どうか**は別
- **件数を人数に読み替えない。数字は「何を数えたものか」まで書く。**分母・期間・定義が曖昧なまま置かない
- **桁だけを書かない。必ず単位を付ける。**"seven figure" を「7 桁」と訳すと日本語の読者は円で読む。「7 桁ドル（100 万ドル以上）」のように、一度は実数に開いて添える
- **外部プロトコルに URL を張るときは公式ドメインを毎回確認する。**うろ覚えで張らない。使った先は `glossary.md` に記録する

## 公開

公開元は `main` ブランチの `docs/`。GitHub Pages を **`main` / `/docs`** から配信する設定にしてある前提（初回だけリポジトリの Settings → Pages で手動設定が要る）。公開 URL は <https://shiodome47.github.io/cardano-note/>。

push すれば数分で反映される。`episodes/` と `shared/` は Pages では配信されない。**設定済みかどうかは推測しない。**この環境からは `github.io` に接続できないので、確認するなら `pages build and deployment` のワークフローの実行履歴を見る。

## ディレクトリ

```
├── CLAUDE.md
├── README.md
├── episodes/          原本（字幕ファイルと meta.yml。無加工）
├── shared/            回をまたぐ資産
│   ├── series.yml          チャンネルの定義
│   ├── glossary.md         用語・誤変換・翻訳方針
│   ├── speakers.md         話者の特定メモ
│   └── open-questions.md   保留になった問い
├── tools/
│   ├── check.py            機械的な検証
│   └── build_standalone.py 1 枚版の生成
└── docs/              公開する成果物（GitHub Pages のルート）
    ├── index.html     全回の一覧
    ├── assets/style.css
    └── YYYY-MM-DD-<slug>/
```
